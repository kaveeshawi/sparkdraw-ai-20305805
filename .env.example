# ── AI Provider ──
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# ── Service ──
PORT=8001
ALLOWED_ORIGINS=http://localhost:8000,http://localhost:5173

# ── Laravel internal auth (optional shared secret for internal calls) ──
INTERNAL_SECRET=
